{
  "name": "flashy",
  "version": "0.1.0",
  "private": true,
  "description": "Flashy autonomous coding partner with desktop shell",
  "scripts": {
    "desktop:dev": "npm --prefix desktop run dev",
    "desktop:build-backend": "npm --prefix desktop run build:backend",
    "desktop:pack": "npm --prefix desktop run pack",
    "desktop:dist": "npm --prefix desktop run dist"
  }
}
